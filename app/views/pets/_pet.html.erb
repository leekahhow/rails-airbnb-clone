<div class="col-4">
   <div class="card-trip">
      <%= cl_image_tag pet.photo.key%>
        <div class="card-trip-infos">
          <div>
            <h2><%= link_to pet.name, pet_path(pet) %> </h2>
            <p><%= pet.description[0..20] %>... </p>
          </div>
          <h2 class="card-trip-pricing">$<%= pet.price_per_hour %>/hr</h2>
        </div>
    </div>
</div>
